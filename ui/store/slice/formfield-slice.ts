import { createSlice } from "@reduxjs/toolkit";

export const formFieldSlice = createSlice({
  name: "formField",
  initialState: {},
  reducers: {},
});

export const {} = formFieldSlice.actions;
export default formFieldSlice.reducer;
